Concepts-
- useRef- 
  -does not cause re rendering at change, stores a mutable value -returns obj current
  -access DOM elem directly by using current
  -pointed to an elem by using ref attribute
  -persist useRef value between state changes

-useEffect-
  -sideeffects- fetching data, DOM manipulation, timers, event listeneres
  -runs on every render
  -dependencies passed-
    -useEffefct(() => {}) - runs on every render
    -[] -runs only once on the 1st render
    -[prop, state] -runs on 1st render and on every time the prop/state changes
  -cleanup
    -to prevent memory leaks
    useEffect(() => {
      const timer = setTimeout(() => {
        setCount((count) =>  count+1);
      },1000);
      return () => clearTimeout(Timer)
    },[])
    